# Copilot Instructions

## Project Guidelines
- User reported that MathF does not contain a definition for Clamp; prefer recommending Math.Clamp or a simple manual clamp when suggesting code.
- Do not add comments in english or any other language, unless the user explicitly asks for them.
- When suggesting code, ensure that it is compatible with C# 14.0 and .NET 10.0, as specified in the project file.
- When suggesting code, ensure that it is compatible with the target framework specified in the project file (net10.0).
- When suggesting code, ensure that it does not rely on any features or libraries that are not available in .NET 10.0 or C# 14.0.
- When suggesting code, ensure that it does not rely on any features or libraries that are not available in the target framework (net10.0).
- When suggesting code, ensure that it does not rely on any features or libraries that are not available in the project file (REDACTED_PROJECT_NAME.csproj) and is compatible with C# 14.0 and .NET 10.0.